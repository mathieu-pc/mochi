[tox]
envlist = py313
isolated_build = true

[gh-actions]
python =
	3.13: py313

[testenv]
setenv =
	PYTHONPATH = {toxinidir}
deps =
	-r{toxinidir}/requirements-dev.txt
commands =
	pytest --basetemp={envtmpdir}